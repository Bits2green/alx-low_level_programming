#include <stdio.h>

/**
 * main - Prints alphabet in lowercase and then in uppercase
 * Return: Always success (0)
 *
 */

int main(void)
{
	char a = 'a';
	char b = 'A';

	while (a <= 'z')
	{
		putchar(a);
		a++;
	}
	while (b <= 'Z')
	{
		putchar(b);
		b++;
	}
	putchar('\n');
	return (0);
}
